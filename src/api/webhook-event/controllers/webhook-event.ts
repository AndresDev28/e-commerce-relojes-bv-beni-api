/**
 * webhook-event controller (placeholder)
 *
 * [GAP-1 PR1+2] No public REST surface; this stub keeps Strapi's
 * controller factory happy until/unless a future admin endpoint is added.
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::webhook-event.webhook-event' as any);
